#include<unistd.h>

/**
* _putchar - writes the character c to student
@c: The Character it to print
*
* Return: On success 1.
* On err, -1 is returned, an errno is set appropraitely.
*/
int _putchar(char c)
{ 
return (write(1, &c, 1));
}
